## 1. 独立实验目录与数据契约

- [x] 1.1 【我来】创建 `solder_joint_mvp/` 的包、配置、测试和输出目录骨架，并通过目录结构测试确认没有复制 JPG/ZIP
- [x] 1.2 【我来】定义清单、人工复核、阵列网格和 ROI 元数据的 JSON Schema/字段说明，并用最小示例通过格式校验
- [x] 1.3 【我来】补充独立 `.gitignore` 与 README，验证生成的 ROI、预览和本机路径清单不会被 Git 跟踪
- [x] 1.4 【我来】将复核数据契约扩展为 `keep/other_component/reject/uncertain` 并记录目标族名称、同族分数和重复组，验证旧清单迁移与新示例均通过 Schema 校验

## 2. 数据盘点与可复现抽样

- [x] 2.1 【我来】实现数据根目录扫描和稳定样本 ID 生成，使用正常、损坏、缺失三类夹具测试失败隔离和路径追溯
- [x] 2.2 【我来】实现按来源目录分层、固定随机种子和数量上限的抽样器，重复运行测试应产生相同候选顺序
- [x] 2.3 【我来】对现有数据运行只读盘点，输出来源/尺寸/数量摘要，并人工抽查清单路径能打开对应原图
- [x] 2.4 【我来】实现盘符无关的感知摘要、视觉距离和近重复组，使用相同图、亮度变化图和不同图夹具验证分组稳定性
- [x] 2.5 【我来】以 `焊点桥连 (2).jpg` 为锚点实现全局同族排序，组合封装比例、中心区域、边缘和内部网格特征，并验证目标族夹具高于连接器及其他圆阵列负例

## 3. 目标元件族筛选与人工基准

- [x] 3.1 【我来】自动遍历 `DataV1` 并按目标族分数生成最多 120 张、每批 40 张的候选联系表，验证每张缩略图带样本 ID、分数、重复组和来源
- [x] 3.2 【我来】逐批初审候选并填写 `keep/other_component/reject/uncertain` 建议，验证每条建议可追溯到联系表和原图
- [ ] 3.3 【需要你】确认 Codex 初审结果及少量 `uncertain` 边界样本，并确认至少 10 张 `keep` 属于 `焊点桥连 (2).jpg` 同一元件族
- [ ] 3.4 【我来】以近重复组为单位冻结开发集和验证集，运行检查确保同一拍摄对象不会跨集合泄漏
- [ ] 3.5 【需要你】在验证集上修正芯片框、行列数、四角或少量中心点，不画像素掩膜；以至少 10 个可评估芯片为验收条件

## 4. 规则阵列候选检测

- [ ] 4.1 【我来】实现缩略图灰度归一化和多尺度近圆形候选提取，并用合成亮/暗圆阵列测试尺度与极性兼容性
- [ ] 4.2 【我来】实现基于芯片外框、正视程度、半径一致性、二维周期性、间距和密度的目标区域评分，验证目标芯片高于连接器、其他封装、随机圆点与线性排列负例
- [ ] 4.3 【我来】生成候选框、圆心、置信度和拒绝原因的叠加预览，人工抽查预览与 JSON 坐标一致

## 5. 网格拟合与轻量修正

- [ ] 5.1 【我来】实现鲁棒双基向量、行列索引和阵列方向拟合，使用旋转、缩放、轻度透视和含离群点的合成测试验证编号稳定
- [ ] 5.2 【我来】实现缺失网格位置推断及低置信标记，用移除部分圆点的测试验证其他位置编号不发生漂移
- [ ] 5.3 【我来】实现从阵列框、四角、行列数或少量中心修正重新拟合，验证同一复核文件可重复生成相同网格

## 6. ROI 导出与可追溯性

- [ ] 6.1 【我来】实现按网格间距比例裁切和统一尺寸归一化，用正常、边缘越界和缺失位置测试验证填充/跳过策略及尺寸一致性
- [ ] 6.2 【我来】输出 ROI 清单、配置快照、算法版本和叠加预览，抽查任一 ROI 可反查到原图、阵列及行列编号
- [ ] 6.3 【我来】增加可选 LabelMe 点/矩形导出，并用格式读取测试确认现有标注工具可以载入

## 7. 评估与 MVP 决策

- [ ] 7.1 【我来】实现目标元件族候选精确率、其他元件混入率、焊点中心召回率、相对网格间距定位误差和可用 ROI 比例，使用已知答案的测试夹具校验计算口径
- [ ] 7.2 【我来】在冻结验证集上生成汇总报告和失败案例联系表，确认每个失败项可跳转或追溯到原始样本
- [ ] 7.3 【需要你】复核失败案例并决定继续调规则阵列算法、扩大人工样本，或另开侧视/遮挡路线；以书面结论和下一阶段范围为验收物
- [ ] 7.4 【我来】运行完整流水线与测试，记录可复现命令、耗时、依赖版本和最终 Git 可跟踪文件清单
