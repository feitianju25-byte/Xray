# PCB X光缺陷识别训练方案

## 1. 目标

基于现有 PCB X 光图像，训练一个可用于缺陷自动识别的模型，优先覆盖当前急迫且更容易落地的缺陷类型，并可逐步扩展到提示卡中的全部 10 类缺陷。

## 2. 现有资料概况

### 2.1 缺陷定义来源

依据《X光-检验提示卡1（公开）.docx》，当前文档给出的 10 类常见缺陷为：

1. 过锡不良
2. 焊点空洞
3. 焊点少锡
4. 多余物
5. 漏焊
6. 元器件与焊盘不匹配
7. 焊点拉尖
8. 焊点开裂
9. 器件引脚偏出焊盘
10. 焊点桥连

文档中目录标红的类别，代表当前认为“识别容易且急迫需求待自动识别”的重点类。

### 2.2 当前图像数据

已检查到的图像目录主要分为：

- `X光检测图片新 - 标记`
- `X光检测图片新-未标记`

当前数据根目录下共有 5 个主要数据源：

- `X光图片20260330`：742 张
- `X光图片`：220 张
- `标注缺陷照片20260603`：200 张
- `缺陷照片20260603`：189 张
- `X光图片202605`：146 张

整个目录递归统计到 1,497 张可直接读取的图像：

- 1,486 张 JPG
- 11 张 PNG

其中已确认的类别文件夹包括：

- `过锡不良` / `过锡不足`
- `焊点空洞`
- `焊点少锡`
- `多余物`
- `桥连` / `焊点桥连`
- `合格`

因此，数据中实际已经包含正常样本，不应再按“纯缺陷集”理解。

### 2.3 当前样本分布

按目录统计，当前已确认的关键数据分布为：

- `X光图片20260330`：合格 673 张，缺陷 69 张
- `X光图片`：带标记图 110 张，未标记图 110 张
- `标注缺陷照片20260603`：多余物 84 张、过锡不良 47 张、焊点空洞 37 张、桥连 25 张、焊点少锡 7 张
- `缺陷照片20260603`：多余物 73 张、过锡不良 47 张、焊点空洞 37 张、桥连 25 张、焊点少锡 7 张
- `X光图片202605`：多余物 146 张

现有目录中只有 14 份 LabelMe JSON 框标注文件。`标记`、`未标记`、`标注缺陷照片` 和 `缺陷照片` 很可能包含原图、标记图或重复图，不能直接把文件数等同为可独立训练的样本数。

类别不平衡仍然明显，尤其“焊点少锡”和“桥连”样本偏少；同时需要排查各数据源之间的重复图。

## 3. 任务定义建议

### 第一阶段

建议先做 **目标检测**，而不是整图分类。

原因：

- 缺陷通常只占图像局部区域
- 同一张图可能存在多个缺陷点
- 现有标注文件是矩形框格式，天然适合检测任务

### 第二阶段

在检测模型稳定后，再考虑：

- 多缺陷细分类
- 缺陷严重度分级
- 与产线检验提示联动

## 4. 类别与标签统一建议

建议统一标签命名，避免同义词混用：

- `过锡不足` 统一对应文档中的 `过锡不良`
- 其余类别保持现有文件夹命名

推荐最终训练标签：

- `over_solder`
- `void`
- `insufficient_solder`
- `foreign_object`
- `missing_solder`
- `mismatch`
- `solder_point_sharp`
- `crack`
- `pin_offset`
- `bridge`

如果第一阶段只做当前已有样本，建议先训练 5 类：

1. 过锡不足
2. 焊点空洞
3. 焊点少锡
4. 多余物
5. 焊点桥连

## 5. 数据处理方案

### 5.1 标注格式

现有 JSON 看起来是 LabelMe 风格矩形框标注，建议统一转换为 YOLO 检测格式：

- `images/train`
- `images/val`
- `images/test`
- `labels/train`
- `labels/val`
- `labels/test`

### 5.2 切分策略

建议先做感知哈希或特征去重，再按同一板型、同一拍摄序列或同一原图组切分，避免原图和带标记副本同时出现在训练集和验证集里。

推荐比例：

- 训练集 70%
- 验证集 20%
- 测试集 10%

### 5.3 预处理

建议做：

- 统一灰度或三通道灰度复制
- 尺寸归一化
- 去除明显无效黑边
- 保留原始对比度版本

### 5.4 数据增强

建议增强方式：

- 轻微旋转
- 平移
- 缩放
- 亮度/对比度微调
- 高斯噪声

不建议过强增强，避免破坏 X 光缺陷的物理形态。

## 6. 模型路线

### 6.1 推荐基线

优先使用 YOLO 系列检测模型，原因是：

- 和现有项目结构匹配
- 训练与部署成本低
- 对局部缺陷定位友好

建议起步模型：

- `YOLOv8n` 或 `YOLO11n`

### 6.2 训练策略

#### 阶段 A：小模型快速验证

- 用当前 5 类数据先跑通
- 重点看召回率和漏检率
- 找出样本最少的类别表现

#### 阶段 B：补数据后再训练

- 逐步补齐剩余 5 类
- 统一标签体系
- 重新平衡类别分布

#### 阶段 C：部署优化

- 导出轻量权重
- 在现有识别程序中替换当前通用 `yolo11n.pt`
- 调整置信度阈值与 NMS 参数

## 7. 评估指标

建议至少看以下指标：

- mAP50
- mAP50-95
- 各类别 Precision / Recall
- 漏检率
- 误检率

重点关注：

- `焊点桥连`
- `焊点少锡`

因为这两类样本少，最容易出现泛化问题。

## 8. 风险点

1. 类别严重不均衡
2. 有效框标注目前仅有 14 份，不能直接支撑完整检测训练
3. 目录间可能存在重复图或带标记副本，必须在数据切分前去重
4. 有些缺陷本身边界模糊，标注一致性会影响模型上限
5. 不同拍摄角度、曝光、板型差异会带来域偏移

## 9. 建议的下一步

1. 做图像去重和原图/标记图配对，形成“唯一图像清单”
2. 先把 14 份现有 JSON 转换成统一检测标签，并核对其对应原图
3. 将带标记图用于人工复核，补齐其余缺陷图的矩形框标注
4. 将 673 张合格样本纳入训练与测试，用于控制误报率
5. 先训练 5 类缺陷检测基线模型
6. 评估后再扩展到文档中的 10 类

## 10. 推荐结论

现阶段最稳妥的路线是：

**先清点、去重并补齐标注，再做“合格/缺陷筛查 + 5 类缺陷目标检测”；模型稳定后再扩展到 10 类。**
